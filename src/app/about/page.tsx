// src/app/about/page.tsx
'use client';

import React from 'react';
import FitnessSlider from '../components/FitnessSlider/FitnessSlider'; // adjust if needed

const AboutPage = () => {
  return (
    <section>

      {/* Add the FitnessSlider below the about section */}
      <FitnessSlider />
    </section>
  );
};

export default AboutPage;
